{
  "name": "is-in-subnet",
  "version": "1.0.0",
  "description": "Check if an IPv4 or IPv6 address is contained in the given subnet",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "scripts": {
    "test": "npm run prepare && nyc --reporter lcov --reporter text ava ./build/test",
    "prepare": "del-cli build && tsc"
  },
  "engines": {
    "node": ">=6.10.3"
  },
  "keywords": [
    "ip",
    "address",
    "ipv4",
    "ipv6",
    "cidr",
    "networking"
  ],
  "license": "MIT",
  "author": "Nate Silva <nate@natesilva.com>",
  "devDependencies": {
    "@types/node": "^9.4.5",
    "ava": "^0.25.0",
    "del-cli": "^1.1.0",
    "nyc": "^11.4.1",
    "tslint": "^5.9.1",
    "typescript": "^2.7.1"
  }
}
